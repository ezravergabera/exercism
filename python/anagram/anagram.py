def find_anagrams(word, candidates):
    for candidate in candidates:
        target = word
        for letter in candidate:
            if letter in target:
                target

stringgg = "hello"

stringgg.index()